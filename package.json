{
  "name": "mendrix-customlink",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_EXTRA_CA_CERTS=./certs/sectigo-chain.crt netlify dev",
    "build": "node scripts/generate-version.mjs",
    "pretest": "node scripts/generate-version.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "@types/node": "^25.5.0",
    "cross-env": "^10.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
